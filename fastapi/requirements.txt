fastapi==0.111.1
pydantic==2.9.2
ray[default, rllib]==2.34.0
torch==2.1.0
Pillow>=9.4.0
SuperSuit>=3.9.0
numpy==1.24.0 # Pinned becuase of deprecations. Remove once fix is in master.
PettingZoo[butterfly]==1.24.3 # Temporary until we provide a way to keep envs external from this container
